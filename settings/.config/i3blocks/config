# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
command=$HOME/projects/vivien/i3blocks-contrib/$BLOCK_NAME/$BLOCK_NAME
separator_block_width=25
markup=none

[music]
command=$HOME/.local/bin/music-bar.sh $button
interval=10

[mouse]
command=$HOME/.local/bin/solaar.sh $button
interval=60
markup=pango

[volume]
label=♪
interval=once
signal=10

[date-time]
command=date +"%a %d %b %Y %H:%M"
interval=15
